body{
  margin:0;
  padding:0;
  font-family: "Quicksand", sans-serif;

}
:root {
  background-color: #7651a0fe;
}
.nav {
  width: 100%;
  left: 0;
  top: 0;
  z-index: 10;
  display: flex;
  flex-direction: column; 
  box-sizing: border-box;
  background: #4f00778a; 
  padding: 10px 20px;
  backdrop-filter: blur(100px); /* Your Blur Effect */
  margin: 0;
  transition: all 0.4s ease-in-out;
  position: fixed;
}

.nav-container {
    width: 100%;
}

.nav-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
}

.logo1 {
    margin: 0;
    color: white;
    font-family: 'Times New Roman', serif;
    letter-spacing: 5px;
}

.menu-icon {
    color: white;
    font-size: 1.5rem;
    cursor: pointer;
    width: 30px;
}

.spacer { width: 30px; }

/* Collapse Logic */
.nav-collapse {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.5s cubic-bezier(0, 1, 0, 1);
}

.nav-collapse.show {
    max-height: 1000px;
    transition: max-height 1s ease-in-out;
    padding-top: 20px;
}

.nav-links {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    padding-bottom: 20px;
}

.nav-links a {
    color: white;
    text-decoration: none;
    font-family: 'Quicksand', sans-serif;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 0.9rem;
}
.logo1{
  color:goldenrod;
  font-family:'Times New Roman', Times, serif;
  font-weight:600;
  letter-spacing:0.3rem;
  font-size:2rem;
  font-weight:400;
}
video{
  object-fit:cover;
  width: 100%;
  height: 25%;
  display: block;
}
.shirt-container {
  position: relative; 
  width: 100%;
  height: 100vh;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: #775891;
}
.video-wrapper {
  position: relative;
  width: 100%;
  height: 100vh;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
}
.introall {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: 1;
}

.intro {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: 1;
  filter: sepia(100%) saturate(250%) hue-rotate(240deg) brightness(1) contrast(1.1);
}

.video-title {
  position: relative;
  z-index: 2;
  color: #ffffff;
  font-family:'Times New Roman', Times, serif;
  letter-spacing:1.5rem;
  text-transform: uppercase;
  pointer-events: none;
  font-weight: 400;
  font-size:3.5rem;
  margin: 0;
  padding-left: 1.5rem; 
  animation: fadeIn 2s ease-out forwards;
}
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
.src1 {
  display: flex;         
  justify-content: center;
  align-items: flex-start; 
  gap: 0.5rem;            
  width: 100%;
  margin:3rem 0rem; 
}

.latest {
  width: 50%;          
  display: flex;
  flex-direction: column;
  align-items: center;
  cursor: pointer;
}

.latest img {
  width: 100%;          
  height: 15rem;
  object-fit: cover;
}

.product-name {
  color: #fff;
  font-size: 1.2rem;
  margin-top: 0.5rem;
}
.latest1 {
  width: 100%;           
  height: 50rem;        
  object-fit: cover;
  display: block;
  z-index: 1;
}
.suits {
  position: relative;
  width: fit-content;
  display: inline-block; 
  background-color: rgb(55, 10, 62);
  margin-top:1.2rem;
}
.suitsButton {
  position: absolute;
  top: 50%;         
  left: 50%;      
  transform: translate(-50%, 400%);
  background-color: rgba(255, 255, 255, 0.501);
  border: none;
  padding: 10px 20px;
  color:#000000;
  width:15rem;
  height:4rem;
  cursor: pointer;
}
.linking{
  background-color: rgba(244, 244, 244, 0.852);
  border: none;
  margin:0.5rem;
  width:10rem;
  height:3rem;
  cursor: pointer;
}
button:hover {
  background-color: #4848486a;
}
a {
  text-decoration: none;
  color: #000000;
  font-family: 'Times New Roman', Times, serif;
  font-size:1.5rem;
  font-weight: 400;
}
.subheading{
  color: goldenrod;
  font-size:2.3rem;
  font-family:'Times New Roman', Times, serif;
  font-weight: 400;
  text-align: center;
  margin:3rem 0rem 0rem 0rem;
}
.subdetail{
  font-family:'quicksand';
  color:#ffffff;
  text-align: center;
  font-size:1.1rem;
  margin:1rem;
}
.footer-container {
  background-color: #0b0312;
  color: #fff;
  padding: 4rem 2rem 2rem 2rem;
  font-family: 'Times New Roman', Times, serif;
  margin-top: 4rem;
}

.footer-content {
  display: flex;
  flex-wrap: wrap;
  gap: 2rem;
  margin-bottom: 3rem;
}

.footer-section {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.footer-label {
  font-size: 0.8rem;
  letter-spacing: 2px;
  color: #888;
  margin-bottom: 0.5rem;
}

.footer-link {
  color: #fff;
  text-decoration: none;
  font-size: 1rem;
  display: flex;
  align-items: center;
  gap: 10px;
  transition: opacity 0.3s;
}

.footer-link:hover {
  opacity: 0.7;
}

.underline {
  text-decoration: underline;
  text-underline-offset: 4px;
}

.footer-divider {
  border: 0;
  border-top: 1px solid #333;
  margin: 2rem 0;
}

.footer-bottom {
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.5rem;
}

.footer-logos {
  display: flex;
  gap: 3rem;
  font-size: 0.9rem;
  letter-spacing: 3px;
}

.copyright {
  font-size: 0.7rem;
  color: #666;
}

.main-footer-logo {
  font-size: 4rem; 
  margin-top: 2rem;
  letter-spacing: 15px;
  font-weight: 400;
}

/* Container for the whole grid */
.product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap:1.5rem;
    padding:1.2rem;
    max-width: 1200px;
    margin: 0 auto;
}

.product-card {
    background: #e7d1ff;
    transition: transform 0.3s ease;
    text-align: center;
}

.product-card:hover {
    transform: translateY(-5px);
}

.image-wrapper img {
    width: 100%;
    height: 50%;
    object-fit: cover; 
}

.product-info {
    padding: 15px;
}

.product-info h3 {
    font-size: 1.1rem;
    margin: 10px 0;
    color: #333;
}

.price {
    font-weight: bold;
    color: #e44d26;
    font-size: 1.2rem;
}

.buy-btn {
    margin-top: 10px;
    padding: 1.5rem 5rem;
    background-color: black;
    color: white;
    border: none;
    cursor: pointer;
}

@media (min-width: 768px) {
    .latest, .latest1 {
        filter: grayscale(100%);
    }

    .latest:hover, .latest1:hover {
        filter: grayscale(0%);
    }
    .main-footer-logo {
    font-size: 3rem;
    }
    .footer-content {
      flex-direction: column;
      text-align: left;
    }
  }